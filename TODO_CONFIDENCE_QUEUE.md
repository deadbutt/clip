# 置信度与任务队列 TODO

这份 TODO 只覆盖当前最值得继续投入的两条主线：

1. 转录结果的置信度与背景音/疑似幻觉分诊；
2. 多任务队列、阶段级恢复和失败重试。

目标是增强现有流程，不改变 `segments.json`、源稿备份和现有编辑器的基本工作方式。

## A. 置信度与音频分诊

### A1. 数据模型

- [ ] 在 segment 上增加可选的 `confidence` 对象，不影响旧任务读取。
- [ ] 保存 `asr_confidence`、`no_speech_prob`、`avg_logprob`、`compression_ratio`（模型提供时）。
- [ ] 保存可选的 `audio_features`，例如语音概率、能量、静音比例和音乐/游戏音疑似分数。
- [ ] 保存 `quality_flags`，例如 `possible_hallucination`、`background_audio`、`low_confidence`。
- [ ] 置信度字段只作为诊断元数据，不参与字幕文本、时间轴和翻译字段的混用。

### A2. 计算策略

- [ ] 第一版优先复用 Whisper/faster-whisper 已返回的 segment 级指标。
- [ ] 只在已有转录结果上计算轻量音频特征，不增加一次 Whisper 推理。
- [ ] 需要真实波形或 RMS 时，复用已有音频扫描/时间轴分析结果，避免重复解码。
- [ ] 低置信度和非语音判定先做“标记”，再考虑默认过滤，避免误删真实对白。
- [ ] 对重复短句、纯音乐段、无语音高能量段增加疑似幻觉规则。

### A3. 前端呈现

- [ ] 在字幕列表和时间轴上显示低置信度/疑似背景音标记。
- [ ] 提供“只看疑似问题段”筛选。
- [ ] 显示可解释原因，例如“无语音概率高”“重复文本”“背景音疑似”。
- [ ] 允许用户确认/忽略标记，保存人工判断。
- [ ] 先不把复杂分数暴露成必须理解的数值；优先使用颜色、标签和原因。

### A4. 速度与显存要求

- [ ] 默认方案不能新增一次完整 Whisper 推理。
- [ ] 复用模型输出指标时，转录速度基本不变，额外开销主要是少量 JSON 和前端计算。
- [ ] 轻量 RMS/静音扫描通常增加一次音频读取，目标控制在转录耗时的小比例内。
- [ ] 后续若接入音频事件分类模型，必须做可选开关，并提供“不做分诊”的快速路径。
- [ ] 记录转录、分诊、Demucs、pyannote 各阶段耗时，便于判断是否值得保留。

### A5. 与校对、翻译的数据边界

- [ ] 校对默认只发送当前用户可见的字幕文本、说话人和必要上下文。
- [ ] 默认不把置信度数值、音频特征和内部质量标记发送给 LLM，避免增加 token 和提示复杂度。
- [ ] 若未来需要让 LLM 参考问题段，只发送简短标签，例如“该段可能是背景音误识别”，并做成显式选项。
- [ ] 用户修改字幕后，校对必须读取当前最新的 `segments.json`，不能读取旧的转录结果或旧的源稿快照。
- [ ] 校对结果应用前继续按 segment id 和当前文本做校验，避免覆盖用户在校对期间的新修改。
- [ ] 翻译继续以校对后的当前源稿为输入；结构拆分/合并后保留需重译标记。

## B. 任务队列与恢复

### B1. 队列能力

- [x] 增加明确的 queued / running / paused / retrying / completed / failed / cancelled 状态。
- [ ] 支持多个上传文件排队，并限制同时运行的转录、Demucs、pyannote 和渲染任务数量。
- [x] 支持暂停新任务入队和恢复队列。
- [ ] 允许设置全局并发数，并根据 GPU 显存自动收紧并发。
- [ ] 支持任务优先级，至少区分用户主动重跑和普通批量任务。

### B2. 阶段级恢复

- [ ] 为 download / transcribe / postprocess / diarization / proofread / translate / render 分别保存 checkpoint。
- [ ] 已完成转录的任务只重跑校对、翻译或烧录时，不重新加载 Whisper。
- [ ] 已有 Demucs、pyannote 和翻译结果时，优先复用缓存。
- [ ] 服务重启后扫描未完成任务，区分“可恢复”和“需要人工确认”。
- [ ] 任务失败后保留中间产物和日志，不因为失败清理掉可复用结果。

### B3. 重试与错误提示

- [ ] 区分网络超时、API 限流、模型缺失、显存不足、ffmpeg 失败和用户输入错误。
- [ ] 网络类错误支持有限次数的指数退避重试。
- [ ] 模型和配置类错误不自动无限重试。
- [ ] 前端显示失败阶段、错误类别和下一步建议。
- [ ] 支持用户只重试失败阶段，而不是整条流水线重跑。

### B4. 历史和资源管理

- [x] 记录任务创建时间、完成时间、耗时、模型、设备和阶段耗时。
- [ ] 支持按状态、文件名和时间筛选历史任务。
- [ ] 增加任务清理策略，但保留源稿和用户编辑结果的安全确认。
- [ ] 显示当前 GPU/CPU 占用和正在运行的阶段。
- [ ] 取消任务时确保线程池、子进程和临时文件最终释放。

## 关键行为约定

### 置信度是否会显著降低速度

不会，前提是第一版只复用 faster-whisper 已返回的指标，并把额外分析限制为轻量音频特征。真正可能明显拖慢速度的是新增独立模型，例如音频事件分类或第二次 ASR 推理；这类能力必须做成可选开关。

### 校对是否携带置信度

默认不携带。置信度用于前端筛选和人工复核，不应自动变成 LLM 输入。这样不会增加校对 token，也能避免模型把内部评分误解成字幕内容。未来如需针对问题段校对，可以只发送简短、可解释的标签，并由用户主动开启。

### 用户修改后再校对使用哪一版

使用用户当前已经保存的字幕内容。校对任务启动时应读取最新 `segments.json`，其中包括用户刚做的文本修改、拆分、合并和说话人调整；`source_segments.json` 只作为翻译源稿备份和重译基准，不应覆盖用户当前编辑结果。校对结果应用时还要检查 segment id 和文本版本，避免校对期间的新编辑被旧结果覆盖。

