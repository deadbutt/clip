# MOSS-Transcribe-Diarize 真实素材测试计划

这份计划用于验证当前版本的本地视频字幕工作台。测试分为三层：

1. **P0 冒烟**：用短素材确认转录、字幕产物、时间轴、编辑和切片主链路可用。
2. **P1 功能/异常**：确认校对、翻译、缓存、重启、外部字幕、队列和 FFmpeg 异常符合当前实现。
3. **P2 性能/质量**：长视频、多语种、长期运行和模型质量评估。

真实视频只放在本机，不提交 Git。当前建议目录：

```text
testmd/
├─ media/       # 下载的原始视频；按素材编号命名
├─ runs/        # 每次转录的输出
├─ expected/    # 人工抽查记录、参考字幕和关键时间点
├─ reports/     # JSON/文本报告
└─ logs/        # 下载和转录日志
```

## 当前实现的重要边界

- URL 任务会下载人工平台字幕；找到人工字幕时会把它作为文稿并跳过 Whisper。要测试真实 ASR，优先使用本地上传文件，或显式使用 `force_transcribe`。
- `segments.json` 保存 ASR 原始时间；字幕列表、SRT、ASS 可能对句尾增加约 0.5 秒的展示/导出缓冲。抽查时分别记录 `end`、`display_end` 和导出时间。
- 置信度和质量原因显示在字幕列表中，当前时间轴没有独立的置信度标记。模型指标是诊断分数，不是校准概率。
- 未翻译任务的校对目标是当前 `segments.json`；已有翻译时校对目标是 `source_segments.json`，用于保护源稿。
- 切片导出有持久化缓存，整片烧录当前没有同等的“相同参数不重新编码”缓存承诺。
- 任务队列目前由单个 worker 串行处理；切片 FFmpeg 最多同时运行 2 个。当前没有可配置的全局并发数和任务优先级。
- 转录阶段有 `raw_words.json` 断点恢复；翻译、校对、整片烧录中断后回到 `waiting_review`，需要用户重试；Demucs/pyannote 不作为已完成阶段长期缓存。
- 外部字幕同步会比较 SRT/ASS 的修改标记并读取实际发生变化的文件；只修改 ASS 的回归场景必须持续保留。

## 素材选择

第一轮只下载短素材，避免一次性占用大量磁盘和显存。建议顺序：

| 编号 | 类型 | 建议用途 |
|---|---|---|
| S01 | 约 1 分钟、清晰单人中文 | P0 基准、首尾边界、短片导出 |
| S02 | 约 10–15 分钟多人中文采访 | 说话人切换、编辑和播放联动 |
| S04 | 约 10 分钟英文演讲片段 | 英文转录、时间轴、翻译 |
| S07 | 人声与游戏声混合 | 背景音、切片候选、质量标记 |
| S08 或 S09 | 音效/纯音乐 | 幻觉、非语音误识别 |
| S10 | 日语或中英混合采访 | 多语种专项 |
| S06、S13 | 40–60 分钟长素材 | P2 长任务和稳定性，不作为首轮 P0 |

下载时记录 URL/BVID、下载日期、时长、文件大小和 SHA-256。使用项目自带的 `tools/yt-dlp/yt-dlp.exe`，优先选择 480p 或更低分辨率的单文件格式：

```powershell
New-Item -ItemType Directory -Force testmd\media | Out-Null
tools\yt-dlp\yt-dlp.exe --no-playlist --no-write-info-json --no-write-thumbnail `
  -f "worst[height<=480]/worst" `
  -o "testmd/media/S01_%(id)s.%(ext)s" `
  "https://www.bilibili.com/video/BV1rZb76tEBY"
```

若通过应用的 URL 入口下载，要另外记录任务是否显示“平台字幕（人工）· 已跳过转录”。这类任务不能作为 Whisper ASR 结果使用。

## P0：最小真实链路

| 编号 | 内容 | 操作 | 通过标准 |
|---|---|---|---|
| P0-01 | 本地文件基础转录 | 上传 S01，关闭或明确配置说话人后完成转录 | 进入 `waiting_review`；`segments.json`、SRT、ASS 可读；无静默丢段 |
| P0-02 | 非语音抑制 | 转录 S08/S09 | 不出现大量连续重复幻觉；出现结果时有低质量/疑似幻觉标记，不能要求所有非语音结果绝对为空 |
| P0-03 | 时间边界 | 抽查 10 条字幕的音频前后 0.5 秒，并分别查看 JSON 和 SRT/ASS | 普通对白不能明显提前消失；记录原始结束时间与展示/导出结束时间的差异 |
| P0-04 | 质量标记 | 查看 `segments.json` 和字幕列表 | 有模型指标的段落可见 `confidence`/`quality_flags`；原因可解释；时间轴没有标记不算失败 |
| P0-05 | 编辑后保存 | 修改文本、说话人、时间和样式，刷新页面 | `segments.json`、SRT、ASS 同步；刷新后修改仍在；词级 `items` 不被无故清空 |
| P0-06 | 拆分/合并/切片 | 拆分一段、合并相邻段，再导出一个切片 | 切片字幕相对时间从 0 开始；首尾不截断明显对白；SRT、ASS、MP4 和映射 JSON 可定位 |
| P0-07 | 候选去重 | 对 S07 请求规则候选，检查主轨和备选轨 | 主轨不大面积重叠；备选轨数量有限；17 分钟素材不应返回几十个重复窗口 |
| P0-08 | 切片缓存 | 同一时间、名称、字幕和样式连续导出两次，再分别改变时间、文本和样式 | 仅切片第二次命中 `cached=true`；任一输入变化都会重新编码 |

## P1：功能和异常

| 编号 | 内容 | 重点检查 |
|---|---|---|
| P1-01 | 未翻译任务校对 | 先编辑当前稿，再运行校对；应用前修改同一段文本 | 校对读取保存后的稿件；应用时按 segment id 且原文仍匹配才覆盖，不覆盖并发编辑 |
| P1-02 | 翻译版本边界 | 分别测试“首次翻译前编辑”和“已有翻译后重译” | 明确记录实际读取的是当前稿还是 `source_segments.json`；失败保留原字幕 |
| P1-03 | 多人说话人 | S02/S07 检查标签、颜色、筛选和导出 | 记录误标位置和比例；不要要求模型在抢话边界 100% 正确 |
| P1-04 | 重启恢复 | 在转录、校对、翻译、整片烧录中分别重启服务 | 转录有 `raw_words.json` 时可跳过 Whisper；后处理回 `waiting_review` 并提示重试；不能留下永远运行状态 |
| P1-05 | FFmpeg/文件异常 | 暂时不可用 FFmpeg、删除半成品、制造空输入后重试 | 错误可读；空/半成品不能命中缓存；恢复工具后可重新导出 |
| P1-06 | 外部字幕同步 | 单独修改 SRT；单独修改 ASS；同时存在时修改 ASS | SRT 修改应同步；ASS 单独修改的当前行为若不生效，记录为缺陷，不把它判为通过 |
| P1-07 | 队列/取消 | 提交多个本地文件，暂停、恢复、取消，观察任务和临时文件 | 任务按单 worker 顺序处理；取消后不再进入后续阶段；Demucs 子进程最终释放；不要验收不存在的优先级/可配置并发 |
| P1-08 | 批量切片 | 在 UI 选择多个切片，故意让其中一个失败 | 当前行为是失败后停止后续循环；若产品要求“其他切片继续”，应单独作为待实现需求 |
| P1-09 | URL 任务分支 | 测人工字幕、自动字幕、无字幕和强制转录 | 检查 `transcript_source`，确认是否跳过 Whisper；Cookie、下载失败和取消错误可读 |
| P1-10 | 音频时间轴 | 使用带录屏丢帧/PTS 窟窿的素材 | 检测到窟窿时生成 `audio_fixed.wav`；字幕相对视频播放轴不整体前移 |

## P2：性能和专项质量

| 编号 | 内容 | 通过标准 |
|---|---|---|
| P2-01 | 长视频 | S06/S13 各运行一次，记录转录、Demucs、说话人和导出耗时 | 不崩溃、不永久卡住；进度不倒退；磁盘和显存可解释 |
| P2-02 | 多语种 | 英文、中文、日语、中英混合分别运行 | 语言和翻译方向正确；已有双语字幕不会被无意重复翻译 |
| P2-03 | 长期稳定性 | 连续运行多个任务并重复读取/编辑/导出 | 无明显内存增长；segments/clip 缓存有上限；临时文件可控 |
| P2-04 | 格式和路径 | MP4/MKV、纯音频、中文路径、空文件、损坏文件 | 错误可读；有效媒体能生成字幕；无效输入不会留下伪成功任务 |

## 每次运行记录

```text
测试编号：
素材编号：
运行日期：YYYY-MM-DD
输入路径/URL：
源文件 SHA-256：
文件大小/时长：
模型/设备/语言：
是否走平台字幕：
转录/校对/翻译/导出耗时：
是否命中切片缓存：
产物路径：
发现的问题：
复现步骤：
结果：PASS / WARN / FAIL / BLOCKED
```

## 判定原则

- P0 不允许崩溃、死锁、永久卡住、静默丢产物或错误命中缓存。
- 模型识别错误、说话人边界误标和含混音频导致的时间误差，应记录为 `WARN`，除非造成数据丢失或无法编辑。
- 纯音乐/音效测试关注“没有大量连续高置信度幻觉”和“有可解释标记”，不要求 Whisper 在所有非语音上绝对零输出。
- 发现与当前实现不一致的行为时，先标记为 `FAIL/待实现`，不要为了让测试通过而放宽到失去意义。
- 真实媒体测试不能替代 pytest；每次真实测试前先记录 pytest 基线，区分代码回归、模型质量和环境问题。

## 2026-09-09 执行进度

本轮不调用真实 LLM 校对或 AI 精选。校对边界只使用假实现和 pytest；翻译除假实现外，额外使用本地 OPUS-MT 跑通 13 段真实字幕，不产生付费调用。

| 计划项 | 状态 | 证据/备注 |
|---|---|---|
| P0-01 基础转录 | PASS | S01 本地 Web API，13 段，进入 `waiting_review` |
| P0-02 非语音抑制 | PASS/WARN | 8 秒 440Hz 为 0 段；ASR 纯标点输出已直接丢弃；S09 真实复测去重后保留 1 条 `Thank you.` 且标为 `possible_hallucination`，模型仍会在纯音乐上产字 |
| P0-03 时间边界 | PASS/WARN | CLI/Web 句尾缓冲已统一；S01 的 0.10/0.42/0.44 秒原始短段会保留，展示时间延长但模型短时间戳仍需人工听审 |
| P0-04 质量标记 | PASS/WARN | S09 幻觉和 S08 常见游戏播报已进入人工复核标记；无置信度段在 Web 明确显示“质量指标不可用 · 需复核”；规则是启发式，不能代替内容理解 |
| P0-05 编辑保存 | PASS | 文本、样式、时间、ID、词项、置信度和质量元数据往返保存；浏览器边界拖动可撤销 |
| P0-06 拆分/合并/切片 | PASS | API 拆分合并往返；切片时间从 0 开始 |
| P0-07 候选去重 | PASS | S07 生成主轨 6、备选 3，主轨无重叠；合并后 246s 候选的 reason 已按实际时长重算 |
| P0-08 切片缓存 | PASS | 同一请求第一次未命中、第二次命中缓存 |
| P1-01 校对版本保护 | PASS（假实现） | 现有测试覆盖原文匹配和 segment id，未调用真实 LLM |
| P1-02 翻译版本边界 | PASS | 假实现确认 source backup 边界；本地 OPUS-MT 在 CPU 上翻译 S01 的 13 段约 0.46s，双语导出校验 0 问题 |
| P1-03 真实说话人 | PASS/WARN | 修复短段过滤后，S01 真实 pyannote 保留全部 13 段和 0.10s `Yeah?`，识别 2 人；长多人素材准确率仍未人工标注 |
| P1-04 重启恢复 | PASS/WARN | 52.6 分钟、14,436 词 checkpoint 从 `transcribing` 重启后约 2.4s 恢复为 507 段，未重跑 Whisper；真实 Whisper 运行中取消约 2.5s 生效且无半成品 |
| P1-05 FFmpeg/文件异常 | PASS | 空/损坏 MP4 均进入 `failed`，`error_kind=invalid_media`，前端提示简洁；底层诊断保留在日志 |
| P1-06 外部字幕同步 | PASS | SRT 修改和 ASS 单独修改均会同步；2026-09-09 Web 冒烟确认 `ass_only_external_edit_synced=true` |
| P1-07 队列/取消 | PASS | 暂停、取消、恢复通过；真实 Whisper 取消约 2.5s 生效且无半成品；Demucs 取消后约 0.2s 退出且无孤立人声文件；应用 lifespan 会关闭 worker |
| P1-08 批量切片 | PASS | 浏览器故障注入后 9 项全部尝试并汇总 9 个失败，不再因第 1 项失败停止；错误提示已压缩 |
| P1-09 URL 分支 | PASS/WARN | B 站 URL 下载、合流、Whisper 成功，中文标题正确解码；本次素材无人工字幕，自动/人工字幕分支仍缺真实样本 |
| P1-10 音频时间轴 | PASS | 合成 1 秒 PTS 窟窿被检测、补静音，任务成功且临时修复音频被清理 |
| P2-01 中长素材 | PASS/WARN | 52.6 分钟低码率素材真实 Whisper 用时 182.22s，539 段覆盖到末尾且无重叠；首周期片头出现边界幻觉，长素材说话人分离未跑 |
| P2-02 多语种 | WARN（模型能力） | S10 中日混合素材前段中文一度输出英文；最终字幕时间轴已规整无重叠，但语言识别和文本准确率仍受模型限制 |
| P2-03 稳定性 | PASS/WARN | 52.6 分钟转录和 checkpoint 恢复完成，恢复会带回 `raw_metrics.json` 质量指标；真实 Whisper 中断 2.5s 内取消且无半成品；长列表正常；JobManager 现可关闭，Demucs 取消可回收。持续 RAM/VRAM 曲线仍未采集 |
| P2-04 格式和路径 | PASS | 中文文件名、中文 URL 标题、MP4、MKV、M4A、WAV 通过；空/损坏媒体能正确分类和提示 |

本轮最终基线：完整测试 `235 passed, 1 warning`；Ruff 通过；`node --check` 通过。唯一警告来自 FastAPI `TestClient` 对旧 httpx 接口的第三方兼容层。真实 URL 下载约 2.8 MB，中文标题正确；Web 冒烟再次验证 13 段、切分合并、片段缓存和 ASS 外部同步；真实 Whisper 取消复测通过。`testmd` 当前约 96.13 MiB，没有调用真实 AI 校对、AI 精选或任何付费 API。详细结论见 `testmd/reports/EXECUTION_REPORT.md`。
